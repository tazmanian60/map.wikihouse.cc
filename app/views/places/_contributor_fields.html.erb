<%= form.input :name, autofocus: true %>
<%= form.input :description, label: "Bio", as: :text, hint: "Keep it brief", input_html: { maxlength: 1000 } %>
<%= form.input :links, label: "Links", as: :text, hint: "One on each line. Include your website, social media profiles, etc." %>
